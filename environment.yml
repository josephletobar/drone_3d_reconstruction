name: colmap-reconstruction
channels:
  - conda-forge
# COLMAP is expected from the official CUDA Windows bundle at tools/colmap.
dependencies:
  - python=3.12
  - pip
  - tqdm>=4.66
  - pillow
  - numpy
  - ffmpeg
  - pyvista>=0.46
